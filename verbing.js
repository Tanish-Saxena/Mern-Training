var s="string";
var len=s.length;
var ing="ing";
var ly="ly";
if(s.length>=3){
    let result= s.concat(ing);
    console.log(result);
    if(s[len-3=="i"] && s[len-2=="n"] && s[len-1=="g"]){
        let result=s.concat(ly);
    }
}

